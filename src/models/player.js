export class Player {
  /**
   *
   * @param {String} id
   * @param {String} name
   * @param {Number} score
   */
  constructor({ id, name, score }) {
    this.id = id;
    this.name = name;
    this.score = score;
  }
}
