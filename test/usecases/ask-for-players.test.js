import { askForPlayers } from '../../src/usecases';

describe('testing askForPlayers function', () => {
  test('should return the number of players', () => {
    // const totalPlayers = askForPlayers();
  });
});
